/** @type {import('next').NextConfig} */
/* const nextConfig = {};

export default nextConfig;
 */


/** @type {import('next').NextConfig} */
const nextConfig = {
  turbopack: {
    enabled: true
  },
  // other config options...
}

export default nextConfig
